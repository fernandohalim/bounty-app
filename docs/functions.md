# supabase functions log
## query used
```sql
select p.proname as fn,
       pg_get_functiondef(p.oid) as definition
from pg_proc p
join pg_namespace n on n.oid = p.pronamespace
where n.nspname = 'bounty'
order by p.proname;
```

## responses
```json
[
    {
        "fn": "_award_title",
        "definition": "CREATE OR REPLACE FUNCTION bounty._award_title(p_user uuid, p_group uuid, p_group_name text, p_title text, p_emoji text)\n RETURNS void\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'bounty', 'public'\nAS $function$\r\nbegin\r\n  if p_user is null then return; end if;\r\n  insert into bounty.trophies (user_id, group_id, group_name, title, emoji)\r\n  values (p_user, p_group, p_group_name, p_title, p_emoji);\r\n  perform bounty.award_xp(p_user, 100);   -- titles grant XP\r\nend;\r\n$function$\n"
    },
    {
        "fn": "add_friend_to_group",
        "definition": "CREATE OR REPLACE FUNCTION bounty.add_friend_to_group(p_group uuid, p_friend uuid)\n RETURNS void\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'bounty', 'public'\nAS $function$\r\nbegin\r\n  if auth.uid() is null then raise exception 'not authenticated'; end if;\r\n  if not bounty.is_group_member(p_group) then raise exception 'not a member of this group'; end if;\r\n  if not bounty.are_friends(p_friend) then raise exception 'not your friend'; end if;\r\n  if (select status from bounty.groups where id = p_group) <> 'active' then raise exception 'group is closed'; end if;\r\n  insert into bounty.group_members (group_id, user_id, role)\r\n  values (p_group, p_friend, 'member')\r\n  on conflict (group_id, user_id) do nothing;\r\nend;\r\n$function$\n"
    },
    {
        "fn": "are_friends",
        "definition": "CREATE OR REPLACE FUNCTION bounty.are_friends(p_other uuid)\n RETURNS boolean\n LANGUAGE sql\n STABLE SECURITY DEFINER\n SET search_path TO 'bounty', 'public'\nAS $function$\r\n  select exists (\r\n    select 1 from bounty.friendships\r\n    where status = 'accepted'\r\n      and ((requester_id = auth.uid() and addressee_id = p_other)\r\n        or (requester_id = p_other and addressee_id = auth.uid())));\r\n$function$\n"
    },
    {
        "fn": "award_xp",
        "definition": "CREATE OR REPLACE FUNCTION bounty.award_xp(p_user uuid, p_amount integer)\n RETURNS void\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'bounty', 'public'\nAS $function$\r\nbegin\r\n  update bounty.profiles\r\n     set xp = xp + p_amount,\r\n         level = bounty.level_for_xp(xp + p_amount)\r\n   where id = p_user;\r\nend;\r\n$function$\n"
    },
    {
        "fn": "can_view_profile",
        "definition": "CREATE OR REPLACE FUNCTION bounty.can_view_profile(p_other uuid)\n RETURNS boolean\n LANGUAGE sql\n STABLE SECURITY DEFINER\n SET search_path TO 'bounty', 'public'\nAS $function$\r\n  select p_other = auth.uid()\r\n      or bounty.has_friendship(p_other)\r\n      or bounty.shares_group(p_other);\r\n$function$\n"
    },
    {
        "fn": "create_group",
        "definition": "CREATE OR REPLACE FUNCTION bounty.create_group(p_name text, p_kind bounty.group_kind, p_expires_at timestamp with time zone, p_categories bounty.expense_category[], p_invite_max_uses integer DEFAULT NULL::integer, p_invite_expires_at timestamp with time zone DEFAULT NULL::timestamp with time zone)\n RETURNS uuid\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'bounty', 'public'\nAS $function$\r\ndeclare\r\n  v_group uuid;\r\n  v_code  text;\r\n  v_cat   bounty.expense_category;\r\n  v_try   int := 0;\r\nbegin\r\n  if auth.uid() is null then raise exception 'not authenticated'; end if;\r\n  if p_kind = 'temporal' and p_expires_at is null then\r\n    raise exception 'temporal group needs an expiry';\r\n  end if;\r\n\r\n  insert into bounty.groups (name, owner_id, kind, expires_at)\r\n  values (p_name, auth.uid(), p_kind,\r\n          case when p_kind = 'temporal' then p_expires_at else null end)\r\n  returning id into v_group;\r\n\r\n  insert into bounty.group_members (group_id, user_id, role)\r\n  values (v_group, auth.uid(), 'owner');\r\n\r\n  if p_categories is not null then\r\n    foreach v_cat in array p_categories loop\r\n      insert into bounty.group_listened_categories (group_id, category)\r\n      values (v_group, v_cat) on conflict do nothing;\r\n    end loop;\r\n  end if;\r\n\r\n  loop\r\n    v_try := v_try + 1;\r\n    v_code := bounty.gen_invite_code();\r\n    begin\r\n      insert into bounty.group_invites (group_id, code, created_by, max_uses, expires_at)\r\n      values (v_group, v_code, auth.uid(), p_invite_max_uses, p_invite_expires_at);\r\n      exit;\r\n    exception when unique_violation then\r\n      if v_try > 5 then raise; end if;\r\n    end;\r\n  end loop;\r\n\r\n  return v_group;\r\nend;\r\n$function$\n"
    },
    {
        "fn": "gen_invite_code",
        "definition": "CREATE OR REPLACE FUNCTION bounty.gen_invite_code(p_len integer DEFAULT 8)\n RETURNS text\n LANGUAGE plpgsql\nAS $function$\r\ndeclare\r\n  alphabet text := 'ABCDEFGHJKLMNPQRSTUVWXYZ23456789';\r\n  out_code text := '';\r\n  i int;\r\nbegin\r\n  for i in 1..p_len loop\r\n    out_code := out_code || substr(alphabet, 1 + floor(random() * length(alphabet))::int, 1);\r\n  end loop;\r\n  return out_code;\r\nend;\r\n$function$\n"
    },
    {
        "fn": "group_leaderboard",
        "definition": "CREATE OR REPLACE FUNCTION bounty.group_leaderboard(p_group uuid)\n RETURNS TABLE(user_id uuid, display_name text, avatar_id text, total bigint, txns bigint, biggest bigint)\n LANGUAGE plpgsql\n STABLE SECURITY DEFINER\n SET search_path TO 'bounty', 'public'\nAS $function$\r\nbegin\r\n  if not bounty.is_group_member(p_group) then raise exception 'not a member'; end if;\r\n\r\n  return query\r\n  select pr.id, pr.display_name, pr.avatar_id,\r\n         coalesce(sum(e.amount), 0)::bigint,\r\n         count(e.id)::bigint,\r\n         coalesce(max(e.amount), 0)::bigint\r\n    from bounty.group_members gm\r\n    join bounty.profiles pr on pr.id = gm.user_id\r\n    join bounty.groups   g  on g.id  = p_group\r\n    left join bounty.expenses e\r\n           on e.user_id      = gm.user_id\r\n          and e.is_recurring  = false\r\n          and e.category in (select category from bounty.group_listened_categories where group_id = p_group)\r\n          and e.spent_at >= g.created_at\r\n          and e.spent_at <= coalesce(g.expires_at, now())\r\n   where gm.group_id = p_group\r\n   group by pr.id, pr.display_name, pr.avatar_id\r\n   order by 4 desc;\r\nend;\r\n$function$\n"
    },
    {
        "fn": "handle_reciprocal_friend_request",
        "definition": "CREATE OR REPLACE FUNCTION bounty.handle_reciprocal_friend_request()\n RETURNS trigger\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'bounty', 'public'\nAS $function$\r\ndeclare v_existing bounty.friendships;\r\nbegin\r\n  select * into v_existing from bounty.friendships\r\n   where (requester_id = new.requester_id and addressee_id = new.addressee_id)\r\n      or (requester_id = new.addressee_id and addressee_id = new.requester_id)\r\n   limit 1;\r\n\r\n  if found then\r\n    if v_existing.status = 'accepted' then\r\n      raise exception 'already friends';\r\n    elsif v_existing.requester_id = new.addressee_id then\r\n      -- they already requested you → accept theirs, cancel this duplicate insert\r\n      update bounty.friendships set status = 'accepted', responded_at = now()\r\n       where id = v_existing.id;\r\n      return null;\r\n    else\r\n      raise exception 'friend request already pending';\r\n    end if;\r\n  end if;\r\n  return new;\r\nend;\r\n$function$\n"
    },
    {
        "fn": "has_friendship",
        "definition": "CREATE OR REPLACE FUNCTION bounty.has_friendship(p_other uuid)\n RETURNS boolean\n LANGUAGE sql\n STABLE SECURITY DEFINER\n SET search_path TO 'bounty', 'public'\nAS $function$\r\n  select exists (\r\n    select 1 from bounty.friendships\r\n    where (requester_id = auth.uid() and addressee_id = p_other)\r\n       or (requester_id = p_other and addressee_id = auth.uid())\r\n  );\r\n$function$\n"
    },
    {
        "fn": "is_group_member",
        "definition": "CREATE OR REPLACE FUNCTION bounty.is_group_member(p_group uuid)\n RETURNS boolean\n LANGUAGE sql\n STABLE SECURITY DEFINER\n SET search_path TO 'bounty', 'public'\nAS $function$\r\n  select exists (select 1 from bounty.group_members\r\n                 where group_id = p_group and user_id = auth.uid());\r\n$function$\n"
    },
    {
        "fn": "join_group_with_code",
        "definition": "CREATE OR REPLACE FUNCTION bounty.join_group_with_code(p_code text)\n RETURNS uuid\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'bounty', 'public'\nAS $function$\r\ndeclare v_inv bounty.group_invites; v_status bounty.group_status;\r\nbegin\r\n  if auth.uid() is null then raise exception 'not authenticated'; end if;\r\n\r\n  select * into v_inv from bounty.group_invites where code = upper(trim(p_code));\r\n  if not found then raise exception 'invalid invite code'; end if;\r\n  if v_inv.expires_at is not null and v_inv.expires_at < now() then raise exception 'invite expired'; end if;\r\n  if v_inv.max_uses is not null and v_inv.uses >= v_inv.max_uses then raise exception 'invite used up'; end if;\r\n\r\n  select status into v_status from bounty.groups where id = v_inv.group_id;\r\n  if v_status <> 'active' then raise exception 'group is closed'; end if;\r\n\r\n  insert into bounty.group_members (group_id, user_id, role)\r\n  values (v_inv.group_id, auth.uid(), 'member')\r\n  on conflict (group_id, user_id) do nothing;\r\n\r\n  if found then   -- only count a genuinely new join\r\n    update bounty.group_invites set uses = uses + 1 where id = v_inv.id;\r\n  end if;\r\n\r\n  return v_inv.group_id;\r\nend;\r\n$function$\n"
    },
    {
        "fn": "level_for_xp",
        "definition": "CREATE OR REPLACE FUNCTION bounty.level_for_xp(p_xp integer)\n RETURNS integer\n LANGUAGE sql\n IMMUTABLE\nAS $function$\r\n  select greatest(1, floor(sqrt(p_xp::numeric / 100))::int + 1);\r\n$function$\n"
    },
    {
        "fn": "lock_expired_groups",
        "definition": "CREATE OR REPLACE FUNCTION bounty.lock_expired_groups()\n RETURNS void\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'bounty', 'public'\nAS $function$\r\ndeclare r record;\r\nbegin\r\n  for r in select id from bounty.groups\r\n            where kind = 'temporal' and status = 'active' and expires_at < now()\r\n  loop\r\n    perform bounty.lock_group(r.id);\r\n  end loop;\r\nend;\r\n$function$\n"
    },
    {
        "fn": "lock_group",
        "definition": "CREATE OR REPLACE FUNCTION bounty.lock_group(p_group uuid)\n RETURNS void\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'bounty', 'public'\nAS $function$\r\ndeclare\r\n  g        bounty.groups;\r\n  v_board  jsonb;\r\n  v_whale  uuid; v_sniper uuid; v_sticky uuid; v_monk uuid;\r\nbegin\r\n  select * into g from bounty.groups where id = p_group for update;\r\n  if not found or g.status = 'locked' then return; end if;\r\n\r\n  with agg as (\r\n    select pr.id as user_id, pr.display_name, pr.avatar_id,\r\n           coalesce(sum(e.amount), 0)::bigint as total,\r\n           count(e.id)::bigint                as txns,\r\n           coalesce(max(e.amount), 0)::bigint as biggest\r\n      from bounty.group_members gm\r\n      join bounty.profiles pr on pr.id = gm.user_id\r\n      left join bounty.expenses e\r\n             on e.user_id     = gm.user_id\r\n            and e.is_recurring = false\r\n            and e.category in (select category from bounty.group_listened_categories where group_id = p_group)\r\n            and e.spent_at >= g.created_at\r\n            and e.spent_at <= coalesce(g.expires_at, now())\r\n     where gm.group_id = p_group\r\n     group by pr.id, pr.display_name, pr.avatar_id\r\n  )\r\n  select\r\n    jsonb_build_object(\r\n      'locked_at', now(),\r\n      'ranking', coalesce((\r\n        select jsonb_agg(jsonb_build_object(\r\n                 'user_id', user_id, 'display_name', display_name, 'avatar_id', avatar_id,\r\n                 'total', total, 'txns', txns, 'biggest', biggest) order by total desc)\r\n        from agg), '[]'::jsonb),\r\n      'titles', jsonb_build_object(\r\n        'whale',          (select user_id from agg where total   > 0 order by total desc, txns desc limit 1),\r\n        'sniper',         (select user_id from agg where biggest > 0 order by biggest desc limit 1),\r\n        'sticky_fingers', (select user_id from agg where txns    > 0 order by txns desc limit 1),\r\n        'monk',           (select user_id from agg order by total asc, txns asc limit 1))\r\n    ),\r\n    (select user_id from agg where total   > 0 order by total desc, txns desc limit 1),\r\n    (select user_id from agg where biggest > 0 order by biggest desc limit 1),\r\n    (select user_id from agg where txns    > 0 order by txns desc limit 1),\r\n    (select user_id from agg order by total asc, txns asc limit 1)\r\n  into v_board, v_whale, v_sniper, v_sticky, v_monk;\r\n\r\n  update bounty.groups\r\n     set status = 'locked', final_leaderboard = v_board, locked_at = now()\r\n   where id = p_group;\r\n\r\n  perform bounty._award_title(v_whale,  p_group, g.name, 'The Whale',      '🐋');\r\n  perform bounty._award_title(v_sniper, p_group, g.name, 'The Sniper',     '🎯');\r\n  perform bounty._award_title(v_sticky, p_group, g.name, 'Sticky Fingers', '🫳');\r\n  perform bounty._award_title(v_monk,   p_group, g.name, 'The Monk',       '🧘');\r\n\r\n  insert into bounty.group_messages (group_id, type, body)\r\n  values (p_group, 'system', 'Group locked — final results are in! 🏁');\r\nend;\r\n$function$\n"
    },
    {
        "fn": "materialize_recurring",
        "definition": "CREATE OR REPLACE FUNCTION bounty.materialize_recurring()\n RETURNS void\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'bounty', 'public'\nAS $function$\r\ndeclare r record; v_today date; v_next date;\r\nbegin\r\n  for r in\r\n    select re.*, pr.timezone\r\n      from bounty.recurring_expenses re\r\n      join bounty.profiles pr on pr.id = re.user_id\r\n     where re.active\r\n  loop\r\n    v_today := (now() at time zone r.timezone)::date;\r\n    v_next  := r.next_occurrence;\r\n    while v_next <= v_today loop\r\n      insert into bounty.expenses (user_id, amount, category, note, is_recurring, recurring_id, spent_at)\r\n      values (r.user_id, r.amount, r.category, r.note, true, r.id,\r\n              (v_next::timestamp at time zone r.timezone));\r\n      v_next := case r.cadence\r\n        when 'weekly'  then v_next + 7\r\n        when 'monthly' then (v_next + interval '1 month')::date\r\n      end;\r\n    end loop;\r\n    update bounty.recurring_expenses set next_occurrence = v_next where id = r.id;\r\n  end loop;\r\nend;\r\n$function$\n"
    },
    {
        "fn": "on_expense_insert",
        "definition": "CREATE OR REPLACE FUNCTION bounty.on_expense_insert()\n RETURNS trigger\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'bounty', 'public'\nAS $function$\r\ndeclare\r\n  v_tz         text;\r\n  v_today      date;\r\n  v_last       date;\r\n  v_streak     int;\r\n  v_longest    int;\r\n  v_limit      bigint;\r\n  v_share      boolean;\r\n  v_week_start timestamptz;\r\n  v_week_total bigint;\r\n  r            record;\r\nbegin\r\n  if new.is_recurring then\r\n    return new;\r\n  end if;\r\n\r\n  select timezone, last_log_date, current_streak, longest_streak\r\n    into v_tz, v_last, v_streak, v_longest\r\n    from bounty.profiles where id = new.user_id;\r\n\r\n  v_today := (now() at time zone v_tz)::date;\r\n\r\n  if v_last is distinct from v_today then\r\n    if v_last = v_today - 1 then\r\n      v_streak := v_streak + 1;\r\n    else\r\n      v_streak := 1;\r\n    end if;\r\n    v_longest := greatest(v_longest, v_streak);\r\n\r\n    update bounty.profiles\r\n       set last_log_date  = v_today,\r\n           current_streak = v_streak,\r\n           longest_streak = v_longest\r\n     where id = new.user_id;\r\n\r\n    perform bounty.award_xp(new.user_id, 15);\r\n    if v_streak % 7 = 0 then\r\n      perform bounty.award_xp(new.user_id, 50);\r\n    end if;\r\n  end if;\r\n\r\n  -- listener fan-out → bounty card in every active group listening to this category\r\n  for r in\r\n    select gm.group_id\r\n      from bounty.group_members gm\r\n      join bounty.groups g  on g.id = gm.group_id and g.status = 'active'\r\n      join bounty.group_listened_categories lc\r\n        on lc.group_id = gm.group_id and lc.category = new.category\r\n     where gm.user_id = new.user_id\r\n  loop\r\n    insert into bounty.group_messages\r\n      (group_id, type, sender_id, expense_id, category, amount, note)\r\n    values\r\n      (r.group_id, 'bounty_card', new.user_id, new.id, new.category,\r\n       new.amount, new.note);\r\n  end loop;\r\n\r\n  -- budget blowout: only on the expense that crosses the weekly limit\r\n  select weekly_limit, share_blowout into v_limit, v_share\r\n    from bounty.budgets where user_id = new.user_id;\r\n\r\n  if v_limit is not null and v_share then\r\n    v_week_start := date_trunc('week', now() at time zone v_tz) at time zone v_tz;\r\n    select coalesce(sum(amount), 0) into v_week_total\r\n      from bounty.expenses\r\n     where user_id = new.user_id and is_recurring = false and spent_at >= v_week_start;\r\n\r\n    if v_week_total > v_limit and (v_week_total - new.amount) <= v_limit then\r\n      insert into bounty.group_messages (group_id, type, sender_id, body)\r\n      select gm.group_id, 'budget_blowout', new.user_id, 'blew their weekly budget 💸'\r\n        from bounty.group_members gm\r\n        join bounty.groups g on g.id = gm.group_id and g.status = 'active'\r\n       where gm.user_id = new.user_id;\r\n    end if;\r\n  end if;\r\n\r\n  return new;\r\nend;\r\n$function$\n"
    },
    {
        "fn": "on_profile_insert",
        "definition": "CREATE OR REPLACE FUNCTION bounty.on_profile_insert()\n RETURNS trigger\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'bounty', 'public'\nAS $function$\r\nbegin\r\n  insert into bounty.budgets (user_id) values (new.id) on conflict do nothing;\r\n  insert into bounty.notification_settings (user_id) values (new.id) on conflict do nothing;\r\n  return new;\r\nend;\r\n$function$\n"
    },
    {
        "fn": "prevent_username_change",
        "definition": "CREATE OR REPLACE FUNCTION bounty.prevent_username_change()\n RETURNS trigger\n LANGUAGE plpgsql\nAS $function$\r\nbegin\r\n  if new.username is distinct from old.username then\r\n    raise exception 'username cannot be changed';\r\n  end if;\r\n  return new;\r\nend;\r\n$function$\n"
    },
    {
        "fn": "respond_friend_request",
        "definition": "CREATE OR REPLACE FUNCTION bounty.respond_friend_request(p_request uuid, p_accept boolean)\n RETURNS void\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'bounty', 'public'\nAS $function$\r\nbegin\r\n  if p_accept then\r\n    update bounty.friendships set status = 'accepted', responded_at = now()\r\n     where id = p_request and addressee_id = auth.uid() and status = 'pending';\r\n  else\r\n    delete from bounty.friendships\r\n     where id = p_request and addressee_id = auth.uid() and status = 'pending';\r\n  end if;\r\n  if not found then raise exception 'request not found'; end if;\r\nend;\r\n$function$\n"
    },
    {
        "fn": "send_friend_request",
        "definition": "CREATE OR REPLACE FUNCTION bounty.send_friend_request(p_identifier text)\n RETURNS text\n LANGUAGE plpgsql\n SECURITY DEFINER\n SET search_path TO 'bounty', 'public'\nAS $function$\r\ndeclare v_target uuid;\r\nbegin\r\n  if auth.uid() is null then raise exception 'not authenticated'; end if;\r\n\r\n  select id into v_target from bounty.profiles where lower(username) = lower(trim(p_identifier));\r\n  if v_target is null then\r\n    select id into v_target from auth.users where lower(email) = lower(trim(p_identifier));\r\n  end if;\r\n  if v_target is null then raise exception 'no user found'; end if;\r\n  if v_target = auth.uid() then raise exception 'cannot add yourself'; end if;\r\n\r\n  insert into bounty.friendships (requester_id, addressee_id)\r\n  values (auth.uid(), v_target);     -- reciprocal / duplicate handled by trigger\r\n  return 'ok';\r\nend;\r\n$function$\n"
    },
    {
        "fn": "shares_group",
        "definition": "CREATE OR REPLACE FUNCTION bounty.shares_group(p_other uuid)\n RETURNS boolean\n LANGUAGE sql\n STABLE SECURITY DEFINER\n SET search_path TO 'bounty', 'public'\nAS $function$\r\n  select exists (\r\n    select 1 from bounty.group_members a\r\n    join bounty.group_members b on a.group_id = b.group_id\r\n    where a.user_id = auth.uid() and b.user_id = p_other);\r\n$function$\n"
    }
]
```